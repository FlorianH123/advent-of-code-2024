export function sum(lhs: number, rhs: number): number {
    return lhs + rhs;
}

export function numberCompare(lhs: number, rhs: number): number {
    return lhs - rhs;
}

export function distance(lhs: number, rhs: number): number {
    return Math.abs(lhs - rhs);
}
